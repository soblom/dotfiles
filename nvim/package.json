{
  "name": "neovim-config-dependencies",
  "version": "1.0.0",
  "description": "NPM dependencies for Neovim configuration (Providers only)",
  "dependencies": {},
  "devDependencies": {
    "neovim": "^5.2.0"
  },
  "scripts": {
    "install-deps": "npm install -g neovim"
  }
}
